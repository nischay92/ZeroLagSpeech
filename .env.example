# Web (copy to apps/web/.env.local when running outside Docker)
NEXT_PUBLIC_API_URL=http://localhost:8000

# API (copy to apps/api/.env when running outside Docker)
APP_ENV=development
API_HOST=0.0.0.0
API_PORT=8000
WEB_ORIGIN=http://localhost:3000

# TODO(phase-2): Add Supabase PostgreSQL connection variables.
# TODO(phase-3): Add Redis connection variables.

